#creating the github repo
mkdir scrapping-test-{condidate_name}
cd scrapping-test-{condidate_name}
git init

#get repo status
git status
#add to stagging state
git add {file_name} / or git add . #to add all the files

#what if we use python to implemt it to django later

#checkout commit check the commint /git checkout
#revert commit back in time /git revert
#reset commit delete commit and reset